#include <iostream>
#include <string>
#include <vector>
using namespace std;
template <typename T>
T frequent(T beg, T end)
{
    int cnt = 0;
    while(beg != end)
    {
        T first = beg;
        while(first != end)
        {
            
        }
    }
}
